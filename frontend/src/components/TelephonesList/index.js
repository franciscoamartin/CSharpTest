import React from 'react';

export default function TelephonesList({ telephones }) {
  return (
    <div className="">
      {telephones.map((tel) => (
        <p>{te.nu}</p>
      ))}
    </div>
  );
}
